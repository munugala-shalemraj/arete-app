// Placeholder test — Supabase requires initialization before widget tests.
// Use integration_test package for full E2E tests with a real/mock Supabase.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
